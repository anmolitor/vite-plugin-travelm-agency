{
  "name": "example",
  "version": "1.0.0",
  "description": "example for travelm-agency-vite-plugin usage",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start:inline": "cd inline && vite",
    "build:inline": "cd inline && vite build",
    "preview:inline": "cd inline && vite preview",
    "start:dynamic": "cd dynamic && vite",
    "build:dynamic": "cd dynamic && vite build",
    "preview:dynamic": "cd dynamic && vite preview"
  },
  "author": "Andreas Molitor",
  "license": "ISC",
  "devDependencies": {
    "vite-plugin-elm": "^3.0.0"
  }
}
